<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

## UI & UX Design System Directive
Whenever adding, updating, or modifying UI/UX components, layouts, or styling in this workspace, **ALWAYS** reference and adhere strictly to the guidelines defined in [`Frontend/design.md`](file:///c:/Users/pruth/OneDrive/Desktop/vscodes/RICE%20agent/Frontend/design.md) (**Tactile Precision Dark**).

## Animation & Motion Directive
This project uses **`motion`** (formerly framer-motion), **`gsap`** (with **`@gsap/react`**), and **`lenis`** for scrolling, transitions, and motions. Whenever asked, the active agent **MUST** walk the user through these libraries, their components, and their features, and explain how to apply them to build high-end interactive neumorphic designs. Refer to [animations.md](file:///c:/Users/pruth/OneDrive/Desktop/vscodes/RICE%20agent/.agents/rules/animations.md) for more details.

